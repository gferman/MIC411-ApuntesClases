\chapter{Vibraciones Estocásticas}

\section{Proceso Estocástico}
Una \underline{variable aleatoria} $X$ es una variable que contiene un grado de incertidumbre. Se caracteriza por una distribución (ej. Distribución normal o lognormal). Una ``realización'' de una variable aleatoria toma un único valor. \\

Ejemplos de variables aleatorias:
\begin{itemize}
    \item Período de retorno (``ocurrencia'') de un terremoto % $ > M_w8.5$
    \item Módulo de Young de un material (Hormigón: $E_c$)
    \item Resistencia característica (Acero: $F_y$)
    \item Geometría
    \item Intensidad de un terremoto en un sitio específico.
\end{itemize}

Un \underline{proceso estocástico} (p.e.)  $\mathbb{X}$ es una colección de variables aleatorias para cada $t \in T$. Para los casos prácticos de este curso, la indexación $t$ puede ser el tiempo, por lo que la colección es secuencial. Además, un p.e. puede ser continuo (imposible de hacer en computador) o discreto (conjunto, arreglo o vector).
\begin{align*} 
    \Longrightarrow \mathbb{X} = \{X(t): t \in T\} =\{X(t_1), X(t_2), ..., X(t_m)\}
\end{align*}

Una realización de un proceso estocástico corresponde a la realización de todas las variables aleatorias de su colección. \\

Realización 1: 
\begin{figure}[H]
\centering
\includegraphics[scale=0.7]{Figuras/pe.png}
\end{figure}

Realización 2: 
\begin{figure}[H]
\centering
\includegraphics[scale=0.7]{Figuras/pe2.png}
\end{figure}

\par Ejemplos de p.e. para aplicación en ingeniería estructural:

\begin{itemize}
    \item Vibraciones aleatorias
    \item Registro de aceleración de un terremoto
    \item Presión del viento
    \item Carga de tráfico en un puente (vehiculo, peatonal)
\end{itemize}

\section{Caracterización de Proceso Estocástico}

\par Existen 2 formas de describir un p.e. $\mathbb{X} = \{X(t)$, $t\in T\}$:

\begin{itemize}
    \item \textbf{Función de densidad de probabilidad acumulada conjunta}\\
    
    La función de densidad de probabilidad acumulada conjunta (\emph{joint CDF} en inglés) de un proceso estocástico se define como la probabilidad conjunta de que el proceso tome ciertos valores en diferentes instantes de tiempo. 
    
    Matemáticamente, se expresa como:
    
    \begin{align*}
         F_{X}({x(t)}) &= \mathbb{P}[X(t) \leq {x(t)}]\\
        \Longrightarrow F_{X}(x_1,x_2,\ldots,x_m) &= \mathbb{P} \left[ X(t_1)\leq x_1 \cap X(t_2) \leq x_2 \cap \cdots \cap X(t_m) \leq x_m \right]
    \end{align*}    
    
    % Si el p.e es visto como colección de variables aleatorias que depende del tiempo:
    
    % \begin{align*}
    %     \mathbb{P}[X(t_1)\leq x_1 \cap X(t_2)\leq x_2 \cap ... \cap X(t_m) \leq x_m ] = F_{X}(x_1,x_2,...,x_m;t_1,t_2,...,t_m) 
    % \end{align*}
    
    Esta expresión nos proporciona la probabilidad de que el proceso estocástico $X(t)$ en los tiempos $t_i$ tome valores que no excedan $x(t_i)=x_i$.

    Mientras, la función de densidad de probabilidad conjunta (\emph{joint PDF} en inglés) se define como:

    \begin{equation*}
         f_{X}({x(t)}) = \frac{\partial^m F_X(x_1,x_2,\ldots,x_m)}{\partial x_1 \partial x_2 \cdots \partial x_m} 
    \end{equation*}
    
    \item \textbf{Momentos estocásticos}\\ 
    \underline{Recordatorio}: \\
    El valor esperado de una variable aleatoria $\mathbb{E}\{X\}$ es la integral del producto entre los valores que puede tomar esa variable aleatoria $x$ y la función de distribución de probabilidad evaluada en ese valor $f_X(x) = \mathbb{P}(X=x)$.
    
    \begin{align*}
        \mathbb{E}\{X\} = \int_{-\infty}^{\infty} x f_X(x) dx
    \end{align*}
    
    En el caso discreto $p_X(x_i) = \mathbb{P}(X=x_i)$:
    
    \begin{align*}
        \mathbb{E}\{X\} = \sum_{i=1}^{\infty} x_i p_X(x_i) 
    \end{align*}

    En MATLAB, se puede ocupar \texttt{mean(X)} donde \texttt{X} es un vector de números sampleados con la distribución de su correspondiente variable aleatoria $X$. \\ 
    
    Los momentos estadísticos de 1er y 2do orden son los siguientes:
    \begin{itemize}
        \item Media $(m \times 1)$: 
        \begin{align*}
            \mu_{X}(t_i)=\mathbb{E}\{X(t_i)\}
        \end{align*}
    
        \item Varianza $(m \times 1)$:
        \begin{align*}
            \sigma_{X}^2(t_i)=\mathbb{E}\{[X(t_i)-\mu_X(t_i)]^2\}
        \end{align*}
    
        \item Función de autocorrelación $(m \times m)$: 
        \begin{align*}
            R_{XX}(t_i,t_j)=\mathbb{E}\{X(t_i)X(t_j)\}
        \end{align*}
    
        \item Función de autocovarianza $(m \times m)$: 
        \begin{align*}
            \Gamma_{XX}(t_i,t_j)&=\mathbb{E}\{[X(t_i)-\mu_X(t_i)][X(t_j)-\mu_X(t_j)]\} \\
            \Gamma_{XX}(t_i,t_j)&=R_{XX}(t_i,t_j)-\mu_{X}(t_i) \mu_{X}(t_j)
        \end{align*}

        Nota: $\sigma_{X}^2(t_i) = \Gamma_{XX}(t_i,t_i)$
    
        \item Coeficiente de correlación $(m \times m)$: 
        \begin{align*}
            \rho_{XX}(t_i,t_j) = \frac{\Gamma_{XX}(t_i,t_j)}{\sigma_{X}(t_i)\sigma_{X}(t_j)}
        \end{align*}

        Notar que: $\rho_{XX}(t_i, t_i) = 1$.\\
        
        Interpretación de la correlación: ``medida de independencia lineal entre variables aleatorias''.
        
        \begin{figure}[H]
            \centering
            \subfigure[]{\includegraphics[scale=0.55]{Figuras/rho1.png}}
            \subfigure[]{\includegraphics[scale=0.55]{Figuras/rho0.png}}
        \end{figure}
        \begin{itemize}
            \item $|\rho_{\mathbb{X}\mathbb{X}}(t_i,t_j)| \leq 1$
            \item Si $\rho_{\mathbb{X}\mathbb{X}}(t_i,t_j)=0$, $\forall t_i \neq t_j$, entonces el p.e. es un proceso descorrelacionado (por ejemplo: ruido blanco).
        \end{itemize}
    \end{itemize}
    
    En caso de dos procesos estocásticos, $\mathbb{X} = \{X(t)$, $t \in T_X\}$ e $ \mathbb{Y} = \{Y(t)$, $t \in T_Y\}$.
    
        \begin{itemize}
            \item Correlación cruzada: 
            \begin{align*}
                R_{XY}(t_i,t_j)=\mathbb{E}\{X(t_i)Y(t_j)\}
            \end{align*}
            \item Covarianza cruzada:
            \begin{align*}
                \Gamma_{XY}(t_i,t_j) &=\mathbb{E}\{ [X(t_i)-\mu_x(t_i)][Y(t_j)-\mu_y(t_j)]\} \\
                \Gamma_{XY}(t_i,t_j) &= R_{XY}(t_i, t_j)-\mu_{X}(t_i)\mu_{Y}(t_j)
            \end{align*}
            \item Coeficiente de correlación cruzado: 
            \begin{align*}
                \rho_{XY} (t_i,t_j) =
                \frac{\Gamma_{XY}(t_i,t_j)}{\sigma_{X}(t_i)\sigma_{Y}(t_j)}
            \end{align*}
        \end{itemize}
\end{itemize}

\section{Clasificación de P.E.}
\subsection{Estacionariedad}
\begin{enumerate}
    \item \underline{P.E. no estacionario}: La distribución de probabilidad del p.e. depende del tiempo. Las variables aleatorias de la colección no tienen la misma distribución.
    
    \begin{figure}[H]
    \centering
    \includegraphics[scale=0.7]{Figuras/penoesatacionario.png}
    \end{figure}
    
    \item \underline{P.E. Estrictamente estacionario}: La distribución de probabilidad del p.e. no cambia con un retraso en el tiempo:
    \begin{equation*}
        F_{X}(x_1,...,x_m; t_1,...,t_m) = F_{X}(x_1,...,x_m; \textcolor{brown}{t_1 + \tau,...,t_m + \tau})
    \end{equation*}
    \begin{figure}[H]
    \centering
    \includegraphics[scale=0.7]{Figuras/peestricto.png}
    \end{figure}
    \item \underline{P.E. estacionario en el sentido amplio (\emph{wide-sense stationary})}: Estadísticos de 2do orden son finitos y no varían en el tiempo
    \begin{align*}
        \mu_{X}(t) = \text{cst}, \quad \sigma_{X}^2(t) = \text{cst}, \quad R_{XX}(t_i,t_j) = R_{XX}(\underbrace{t_j-t_i}_{=\tau}) = R_{XX}(\tau)
    \end{align*}
\end{enumerate}

\subsection{Ergocidad}

Un proceso ergódico es aquel en el que los promedios temporales de sus realizaciones son iguales a los promedios estadísticos sobre el conjunto de todas las posibles realizaciones del proceso.

Sea $\mathbb{X}=\{X(t): t\in T\}$ un p.e. estacionario. Se define una media en el tiempo de una función $g(x(t))$: 

$$ \overline{g(X(t))} = \lim_{T \to \infty} \frac{1}{T} \int_0^T g(x(t)) dt$$

Y la media en el conjunto de la función $g(x(t))$:

$$ \mathbb{E}\left[g(X(t))\right] = \int_{-\infty}^{\infty} g(x(t)) f_X(x(t)) dx$$

Entonces, se dice que es \emph{ergódico} ssi la media en el tiempo es igual a la media en el conjunto:

$$ \overline{g(X(t))} = \mathbb{E}\left[g(X(t))\right]$$

En específico, un proceso estocastico se define como:

\begin{description}
    \item[Ergódico en la media.] Si la media estimada en el tiempo:
    
    $$\overline{X} = \lim_{T \to \infty} \frac{1}{T} \int_0^T x(t) dt$$
    
    \noindent es igual a la media en el conjunto:
    
    $$\mathbb{E}\left[X(t)\right] = \mu_X$$

    \item[Ergódico en la autocovarianza.] Si la autocovarianza estimada en el tiempo:

    $$\overline{\Gamma_X}(\tau) = \lim_{T \to \infty} \frac{1}{T} \int_0^T (x(t)- \mu_X)(x(t+\tau)- \mu_X)^T dt$$

    \noindent es igual a la autocovarianza en el conjunto:
    
    $$\mathbb{E}\left[ (X(t)- \mu_X)(X(t+\tau)- \mu_X)^T \right] = \Gamma_X(\tau)$$.

    \item[Ergódico en el sentido amplio.] Si el p.e. es ergódigo en la media y en la autocovarianza.
    
\end{description}

% $$ \lim_{T \to \infty} \overline{x} = \mu_X$$

% \begin{align*}
%     \overline{\mu_X(t)}&=\mu_X(t) \\ 
%     \\
%     \overline{\mathbb{E}\{X^2(t)\}} &=\mathbb{E}\{X^2(t)\} \\
%     \\
%     \overline{R_X(\tau)} &= R_X(\tau)
% \end{align*}

\section{P.E. Gaussiano}
Un p.e. $\mathbb{X} = \{X(t)$, $t \in T\}$, se llama p.e. Gaussiano si, para cada $t\in T$, las $m$ variables aleatorias $X(t_1)$, $X(t_2)$, $...,$ $X(t_m)$ distribuyen normal.

\begin{align*}
    X_i \sim N\big(\mu_X(t), \sigma_X(t)\big)\\
    \Longrightarrow \mathbb{X} = \{X(t), t \in T\}\sim N\big(\mu_X,\Gamma_{XX}\big)
\end{align*}

donde $\mu_X$ es el vector de las medias de las variables aleatorias del p.e. y $\Gamma_{XX}$ es la matriz de auto-covarianza. \\

\underline{Notas}:
\begin{itemize}
    \item Un p.e. Gaussiano y débilmente estacionario es estrictamente estacionario.
    \item Un p.e. Gaussiano preserva las transformaciones lineales, y esto es extremadamente útil en los problemas de sistemas lineales que se verán más adelante.
\end{itemize}

\section{Propiedades de la función de autocorrelación de un P.E.}

\underline{Definición}: Función de Autocorrelación.\\

Sea $\mathbb{X} = \{X(t), t \in T\}$ un w.s.s (wide sense stationary). La matriz de autocorrelación es constante para mismas diferencias temporales, es decir, que la función de autocorrelación sólo depende de la diferencia de tiempo.

\begin{align*}
    R_{X}(t_i,t_j) = R_X(\underbrace{t_i - t_j}_{= \tau})
\end{align*}

Por lo tanto, se define la \underline{Función de Autocorrelación} $R_X(\tau)$ como:

\begin{align*}
    R_{X}(\tau)=\mathbb{E}\{X(t)X(t+\tau)\}
\end{align*}

\underline{Características de $R_{X}(\tau)$}:

\begin{enumerate}
    \item Es una función par $R_{X}(\tau) = R_{X}(-\tau)$
    \item Es finita
    \item $\left|R_{X} (\tau)\right| \leq R_{X} (0)$
    \item En $\tau=0$, $R_{X} (0)$ entrega el máximo valor de la media cuadrada del p.e. $\mathbb{X} = \{X(t), t \in T\}$:

    $R_{X}(0)=\mathbb{E}\{X(t)^2\}=\sigma^2_X + \mu_X^2$
    
    \item $R_{X}(\tau)$ es una función definida positiva, es decir, para cualquier función $g(t)$:
    \begin{align*}
        g(t_1)^T R_{X}(t_1-t_2)g(t_2)\geq 0
    \end{align*}
    Por lo tanto, $R_{X} (\tau)$ es definida positiva siempre.
    \item $R_{X} (\tau)$ es continua en $\tau$ si el p.e. es contínuo.
\end{enumerate}

\section{Densidad de Poder Espectral}

\subsection{Generalidades}

Primero, debemos establecer la definición adecuada de transformada de Fourier. La convención a utilizar es la que corresponde a ingenierísa, donde la transformada de Fourier se define de la siguiente manera:

\begin{align*}
    X(\omega) &= \mathcal{F}\{x(t)\} &=  \int_{-\infty}^{\infty} x(t) e^{-j \omega t} dt & & & \text{(Transformada adelante, \emph{forward})} \\
    x(t) &= \mathcal{F}^{-1}\{X(\omega)\} &= \frac{1}{2\pi} \int_{-\infty}^{\infty} X(\omega) e^{j \omega t} d\omega & & &\text{(Transformada atrás, \emph{inverse})}
\end{align*}

\noindent donde $j=\sqrt{-1}$ es el número complejo. Notar las siguientes identidades importantes:

\begin{enumerate}
    \item $\mathcal{F}\{\delta(\tau)\} = 1, \quad \forall\omega$
    \item $\mathcal{F}^{-1}\{1\} = 2 \pi \delta(t)$
\end{enumerate}


\subsection{Definición PSD}

\begin{theorem}[Teorema de Wiener-Khinchin]

    Sea $\{X(t): t\in T\}$ un p.e. w.s.s., con función de autocorrelación $R_{X} (\tau)$. La función de densidad de poder espectral (PSD) se define como la transformada de Fourier de la función de autocorrelación:

    \begin{align*}
        S_{X}(\omega) &=\mathcal{F}\{R_{X}(\tau)\}
    \end{align*}
    
    Equivalentemente:
    
    \begin{align*}
        R_{X}(\tau) &=\mathcal{F}^{-1}\{S_{X}(\omega)\}
    \end{align*}

\end{theorem}

\subsection{Propiedades del PSD}

\begin{enumerate}
    \item Función real y par (simétrica).
    % \item Definida positiva.
    \item El \emph{poder} se define como el área bajo la curva de $S_{X}(\omega)$

    $$ P_X = \frac{1}{2\pi}\int_{-\infty}^{\infty} S_X(\omega) $$

    \item El poder del PSD es igual a la autocorrelación $\mathbb{E}\{X(t)^2\} = R_{X}(\tau=0) = \sigma^2_X + \mu^2_X$.
    
\end{enumerate}

\begin{figure}[H]
    \centering
    \includegraphics[scale=0.7]{Figuras/PSD.png}
\end{figure}

\section{Ruido blanco}

Sea $\mathbb{X}=\{X(t): t\in T\}$ un p.e. w.s.s. Gaussiano, con $\mu_X=0$ en todo $t \in T$, $\mathbb{X}$ es un ruido blanco si su PSD es constante:

\begin{align*}
    S_X(\omega) &= S_0, \quad \forall \omega \in (-\infty,+\infty)\\
    \Longrightarrow R_X(\tau) &= S_0\delta (\tau)
\end{align*}

\noindent donde $S_0$ tiene unidades de [(Poder)/(rad/s)]. Por ejemplo: [(m/s$^2$)$^2$/(rad/s)], [(N)$^2$/(rad/s)].

\begin{figure}[H]
\centering
\includegraphics[scale=0.7]{Figuras/ruidoblanco.png}
\end{figure}

\underline{Nota}: Un Ruido blanco es un p.e. Gaussiano y ergódico.

\section{Ruido blanco de banda limitada (BLWN)}

Sea $\{X(t),t \in T\}$ un p.e., w.s.s., es un \emph{ruido blanco de banda limitada} (band-limited white noise, BLWN) si:

\begin{align*}
    S_X(\omega) & = \left\{
\begin{array}{ll}
      S_0 &, |\omega|\leq \omega_c \\
      0 &, |\omega|> \omega_c \\
\end{array} 
\right. \\
    R_X(\tau) & = \left( 2 S_0 \omega_c \right) \left( \frac{1}{2\pi} \right)  \left[ \frac{\sin(\omega_c \tau)}{\omega_c \tau} \right] \\
    & = \left( \frac{S_0 \omega_c}{\pi} \right) \text{sinc}(\omega_c \tau) 
\end{align*}

\begin{figure}[H]
\centering
\includegraphics[scale=0.7]{Figuras/BLWN.png}
\end{figure}

% Nota: a veces este p.e. se conoce como un filtro de paso bajo (``low-pass filter'').

Varianza: $\sigma^2 = R_x(0) = \frac{S_0 \omega_c}{\pi}$.

\begin{figure}[hp]
    \centering
    \includegraphics[width=6in]{Figuras/BLWN_ACF.png}
    \caption{Autocorrelación BLWN, creado en Python.}
\end{figure}

\section{Valores extremos de P.E.}

\emph{En desarrollo} \parencite[Sec. 6.2]{wirsching2006randomvibrations}.

\section{Respuesta estocástica de sistemas lineales}

Sea un sistema LTI:

\begin{align*}
    {\dot{x}} ={A}{x} + {B}u(t), \quad {x}(0)={0}
\end{align*}

y asumiendo que $u(t)$ es una realización de un p.e. $\mathbb{U} = \{U(t), t \in T\}$ w.s.s. con $\mu_\mathbb{U}(t) = 0$. \\

\par \underline{Objetivo}: Determinar el estado ${x}(t)$, el cual se puede analizar como un p.e. w.s.s., $\mathbb{X} = \{X(t)$, $t \in T\}$ con $\mu_X(t)=0$\\

Recordar que la autocovarianza ${\Gamma}_X(t)$ cuando $\mu_X(t)=0$ es:

\begin{align*}
    \Gamma_X(t) = \mathbb{E}\{X(t)X(t)^T\}
\end{align*}

Entonces, la derivada temporal de la autocovarianza es:

\begin{align*}
    \dot{\Gamma}_X(t)&=\frac{d}{dt}\mathbb{E}\{XX^T\}=\mathbb{E}\{\frac{d}{dt}(XX^T)\}\\
    &= \mathbb{E}\{\dot{X}X^T+X\dot{X}^T\}\\
    &= \mathbb{E}\{(AX+BU)X^T+X(AX+BU)^T\}\\
    &= A\mathbb{E}\{XX^T\}+B\mathbb{E}\{UX^T\}+\mathbb{E}\{XX^T\}A^T+\mathbb{E}\{XU^T\}B^T
\end{align*}

Definiendo $Q=\mathbb{E}\{BUX^T\}+\mathbb{E}\{XU^T B^T\}$
\begin{align*}
    \dot{\Gamma}_X &= A\Gamma_X+\Gamma_XA^T+Q \qquad \text{(Ecuación diferencial de Lyapunov)}
\end{align*}

Si ${u}(t)$ es un ruido blanco, es decir, $\mu_U=0$, $R_U(\tau)= S_o\delta (\tau)$, con $S_U(\omega)=S_0, \forall \omega$, y recordando que la solución analítica de $x(t)$ es:

\begin{equation*}
    x(t)=\phi(t)x_0+\int_0^t \phi(t-\tau)Bu(\tau)d\tau
\end{equation*}

Desarrollemos el primer término de $Q$ de la ecuación diferencial de Lyapunov:

\begin{align*}
    \mathbb{E}\{BUX^T \} &=  \mathbb{E} \left\{ BU(t) \left[ \int_0^t \phi(t-\tau)B U(\tau) d\tau \right]^T \right\}\\
    &= B \int_0^t \mathbb{E} \left\{U(t) \left[U^T (\tau) B^T \phi^T (t-\tau) \right] \right\} d\tau\\
    &= B \int_0^t \mathbb{E} \left\{ U(t)U^T(\tau) \right\} B^T \phi^T (t-\tau) d\tau
\end{align*}

Dado que $U(t)$ es un ruido blanco con  $\mu_{U}(t)=0$, $R(\tau) = \mathbb{E}\{U(t)U(t +\tau)\} = S_0 \delta (\tau)$. También, se cumple: $\mathbb{E}\{U(t)U(\tau)\} = S_0 \delta (t-\tau)$. Entonces, se puede demostrar que:

\begin{align*}
    \mathbb{E}\{BUX^T \}=\frac{1}{2}B (S_0) B^T
\end{align*}

Por lo tanto:
\begin{align*}
    Q &= \mathbb{E}\{BUX^T \}+\mathbb{E}\{BU^T X^T \}=2\mathbb{E}\{BUX^T \}\\
    Q &= B(S_0)B^T
\end{align*}

Para resolver $\Gamma_X(t)$, se calcula $Q$ que depende de $S_0$ y $B$:

\begin{align*}
    \dot{\Gamma}_X(t)=A\Gamma_X+\Gamma_XA^T +Q
\end{align*}

La solución es única ssi $A$ es Hurwitz.\\

\par \underline{Notas}:
\begin{itemize}
    \item $Q$ es una matriz simétrica, definida positiva. Entonces, $s_0$ es una matriz simétrica, definida positiva.
    \item $\Gamma_X$ es una matriz simétrica, definida positiva.
\end{itemize}

\subsection{Autocovarianza caso estacionario}
Estudiando el caso de la respuesta en estado estacionario para $t \to \infty$:

\begin{align*}
    \lim_{t \to \infty} \dot{\Gamma}_X(t)=0
\end{align*}

En este caso, la Ecuación de Lyapunov queda de la siguiente forma:

\begin{align*}
   0 = A\Gamma_X + \Gamma_XA^T + Q, \qquad \text{(Ecuación de Lyapunov)}
\end{align*}

Donde ahora $\Gamma_X$ es la autocovarianza en estado estacionario de los estados $X$.\\

\subsection{Autocovarianza de la respuesta}
La autocovarianza de la respuesta $\Gamma_X$, considerando:
\begin{align*}
    y(t) = Cx(t)
\end{align*}

donde $y(t)$ es un p.e., w.s.s., $\mathbb{Y} = \{Y(t), t \in T\}$ con $\mu_Y(t)=0$.\\

La matriz de autocovarianza de la respuesta se determina de la siguiente manera:
\begin{align*}
    \Gamma_Y = \mathbb{E}\{YY^T\} &= \mathbb{E}\{(CX)(CX)^T\}\\
     &= \mathbb{E}\{CXX^T C^T\}\\
      &= C \mathbb{E}\{XX^T\}C^T\\
       &= C \Gamma_X C^T\\
\end{align*}

Donde $\Gamma_X$ es la solución de la ecuación de Lyapunov en estado estacionario.

\subsection{Error Cuadrático Medio RMS}
Sea el proceso estocástico $\mathbb{X} = \{X(t), t \in T\}$, su Error Cuadrático Medio (Root mean square error, RMS) es una función que cuantifica... y se calcula como:

\begin{align*}
    \text{rms}_X(t) = \sqrt{\mathbb{E}\{(X(t) - \mu_X(t))^2\}}
\end{align*}

Cuando $\mu_X(t) = 0$
\begin{align*}
    & \text{rms}_X(t) = \sqrt{\mathbb{E}\{X(t)^2\}} = \sqrt{\mathbb{E}\{X(t)X(t)^T\}} \\
    \Longrightarrow & \text{rms}_X(t) = \sqrt{\Gamma_{X}(t)} 
\end{align*}


\section{Modelos Estocásticos en Ingeniería Estructural}
Los parámetros de modelos de sistemas dinámico contienen incertidumbre que requiere ser incorporada para un mejor entendimiento estadístico del problema. Algunos parámetros se pueden considerar como variables aleatorias. Otros, como las vibraciones, pueden ser modeladas como procesos estocásticos. Por ejemplo.
\begin{itemize}
    \item Terremoto
        \begin{itemize}
            \item Kanai-Tajimi
            \item Clough-Penzien
        \end{itemize}
    \item Viento
        \begin{itemize}
            \item Davenport
            \item Kaimal-Vokerman
        \end{itemize}
    \item Oleaje
    \begin{itemize}
        \item Pierson-Moskowitz
    \end{itemize}
    \item Rugosidad pavimento
    \item Tráfico (vehicular, peatonal)
    \item Etc.
\end{itemize}

\subsection{Simulación de registros sísmicos sintéticos - Modelo Kanai-Tajimi (K-T)}

El modelo K-T utiliza un p.e. Gaussiano estacionario escalado en función del tiempo para representar la aceleración del suelo producto de un evento sísmico. \\

Consideremos el siguiente suelo.

\begin{figure}[H]
\centering
\includegraphics[scale=0.85]{Figuras/KT.png}
\end{figure}

El suelo es modelado como un sistema SDOF donde la aceleración total del oscilador es lo que representa la aceleración del suelo. En este caso, la EDM del oscilador SDOF, considerando el grado de libertad $z(t)$ (movimiento relativo del suelo respecto a la roca), es la siguiente:

\begin{align*}
    \ddot{z}(t) +2\xi_g\omega_g \dot{z}(t) +\omega_g^2 z(t) = W(t) 
\end{align*}

Considerando la aceleración de la roca base $W(t)$ como p.e. $\mathbb{W} = \{W(t), t \in T\}$ ruido blanco, es 

\begin{align*}
    \mu_W(t) =& 0 \\
    R_W(\tau) =& S_0 \delta(\tau) \\
    S_W(\omega) =& S_0, \quad \forall \omega
\end{align*}

Donde $S_0$ es el poder espectral del sismo en roca y está relacionado con una medida de intensidad del registro sísmico, ej: $PGA$ o $S_a(T)$.\\

La aceleración total del suelo es  $\ddot{u}_{g} = \ddot{z} + w(t)$ (en campo abierto):

\begin{align*}
    \ddot{u}_g(t) = -2\xi_g\omega_g \dot{z}(t) - \omega_g^2 z(t)
\end{align*}

En representación espacio-estado:

\begin{align*}
    \dot{x}_g =& {A}_g{x}_g+{B}_g W(t) \\
    {y}_g =& {C}_g{x}_g
\end{align*}

Donde:

\begin{align*}
    {x_g} = \begin{Bmatrix}
        z \\
        \dot{z}
    \end{Bmatrix}, \qquad
    y_g = \begin{Bmatrix}
        \ddot{u}_g
    \end{Bmatrix}
\end{align*}

\begin{align*}
    {A}_g = \begin{bmatrix}
        0 & 1 \\
        -\omega_g^2  & -2\xi_g\omega_g
    \end{bmatrix}, \qquad
    {B}_g = \begin{bmatrix}
        0 \\
        1
    \end{bmatrix}, \qquad
    {C}_g = \begin{bmatrix}
        -\omega_g^2 & -2\xi_g\omega_g
    \end{bmatrix}, \qquad
    {D}_g = [0]
\end{align*}

Se puede demostrar que la función de respuesta en frecuencia (FRF) del estado de suelo es:

\begin{align*}
    H_{KT}(\Omega) &= \frac{\omega_g^2+i2\xi_g\omega_g\Omega}{(\omega_g^2-\Omega^2)+i2\xi_g\omega_g\Omega}\\
    |H_{KT}(\Omega)|^2 &= \frac{\omega_g^4+4\xi_g^2\omega_g^2\Omega^2}{(\omega_g^2-\Omega^2)^2+4\xi_g^2\omega_g^2\Omega^2}
\end{align*}

\begin{figure}[H]
\centering
\includegraphics[scale=0.045]{Figuras/HKT_2.png}
\end{figure}

Este sistema (filtro K-T) toma como input el ruido blanco (aceleración en roca) y lo colorea generando el output que es el registro sísmico en la superficie.

\begin{figure}[H]
\centering
\includegraphics[scale=0.55]{Figuras/diagrama_bloque_KT.png}
\end{figure}

Se puede demostrar que:

\begin{align*}
    %\ddot{u}_g(t) &= h_{KT} \ast W(t)\\
    %\ddot{u}_g(t)\ddot{u}_g(t+\tau) &= h_{KT}^2(t) \ast W(t)W(t+\tau)\\
    S_{\ddot{U}_g}(\omega) &= |H_{KT}(\omega)|^2 S_{W}(\omega) \\
    \Longrightarrow S_{\ddot{U}_g}(\omega) &= |H_{KT}(\omega)|^2S_0
\end{align*}

Notar que hasta el momento los parámetros del modelo K-T son: $S_0$, $\omega_g$, $\xi_g$. Por ejemplo, los parámetros del terremoto El Centro 1940 son los siguientes \parencite{Soong1993}:

\begin{itemize}
    \item $\bar{\omega}_g = 18$ [rad/s] $ \approx 2.8$ [Hz]
    \item $\bar{\xi}_g = 0.60$
    \item $S_0 = $ depende del PGA del sismo
\end{itemize}

Generalmente los registros de aceleraciones tienen un contenido de frecuencias de hasta $30$ [Hz]. \\

\subsubsection{Filtro K-T aumentado}

Equivalentemente, se puede generar un sistema aumentado (Filtro K-T + Estructura) que tome como input el ruido blanco $W(t)$ y retorne como output la respuesta de la estructura ${y}(t)$.

\begin{figure}[H]
    \centering
    \includegraphics[scale=0.6]{Figuras/estado_estacionario_KT.png}
\end{figure}

Ecuaciones del modelo K-T:

\begin{align*}
    \dot{x}_g(t) &= A_g x_g(t) + B_g W(t)\\
    y_g(t) &= C_g x(t)\\
\end{align*}

Ecuaciones del sistema estructural:

\begin{align*}
    \dot{x}_s(t) &= A_s x_s(t) + B_s \ddot{u}_g(t)\\
    y_s(t) &= C_s x(t)
\end{align*}

El sistema aumentado en representación espacio-estado $x_a = \begin{Bmatrix} x_s \\ x_g \end{Bmatrix}$:

\begin{align*}
    \dot{x}_a &= A_a x_a + B_a W(t)\\
    y &= C_a x_a
\end{align*}

\begin{align*}
    A_a = \begin{bmatrix}
        A_s & B_s C_g \\
        0 & A_g
    \end{bmatrix}, \qquad
    B_a = \begin{bmatrix}
        0 \\
        B_g
    \end{bmatrix}, \qquad 
    C_a = \begin{bmatrix}
        C_s & 0
    \end{bmatrix}
\end{align*}

Sea $\Gamma_{X_a}$ la autocovarianza de los estados del sistema aumentado. Se puede obtener la autocovarianza de la respuesta $\Gamma_Y$ desde la ecuación de Lyapunov en estado estacionario:

\begin{align*}
    0 &= A_a\Gamma_{X_a} +\Gamma_{X_a}A_a^T+Q_a\\
    \Gamma_Y &= C_a\Gamma_{X_a}C_a^T
\end{align*}

\noindent donde $Q_a = B_a \left(S_0 \right) B_a^T$.

\subsubsection{Modelo K-T no estacionario}

El registro sísmico sintético que tenemos hasta el momento $\ddot{u}_g(t)$ es un ruido blanco estacionario. Pero, en realidad los registros sísmicos son p.e. no estacionarios. Por lo tanto, hay que escalarlo con una \emph{función de modulación} $\psi(t)$ para que le de ``forma'' de un registro sísmico.

\begin{figure}[H]
    \centering
    \includegraphics[scale=0.6]{Figuras/func_mod_KT.png}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[scale=0.6]{Figuras/func_mod_KT2.png}
\end{figure}

Entonces:

\begin{align*}
    \ddot{u}_g(t) &= \psi(t) y_{g}(t) \\
    &= \psi(t) \left[ C_g x_g(t) \right] \\
    &= \underbrace{\left[ \psi(t) C_g \right]}_{C_g^{NE}(t)} x_g(t)\\
    &= C_g^{NE}(t) x_g(t)
\end{align*}

Notar que se mantiene $\mu_{\ddot{U}_g}(t) = 0$, pero, $\sigma_{\ddot{U}_g}(t) \neq \text{cst}$. \\

Por lo tanto, el modelo matemático resulta:

\begin{align*}
    \dot{x}_g(t) &= A_g x_g(t) + B_g W(t)\\
    \ddot{u}_g(t) &= C_g^{NE}(t) x(t)\\
\end{align*}

\begin{align*}
    \dot{x}_s(t) &= A_s x_s(t) + B_s \ddot{u}_g(t)\\
    y(t) &= C_s x(t)
\end{align*}


Ahora el sistema aumentado tiene matrices que varían en el tiempo.

\begin{align*}
    \dot{x}_a(t) &= A_a(t)x_a(t) + B_a w(t)\\
    y(t) &= C_a x_a(t)
\end{align*}

\begin{align*}
    A_a(t) = \begin{bmatrix}
        A_s & B_s C_g^{NE}(t) \\
        0 & A_g
    \end{bmatrix}, \qquad
    B_a = \begin{bmatrix}
        0 \\
        B_g
    \end{bmatrix}, \qquad 
    C_a = \begin{bmatrix}
        C_s & 0
    \end{bmatrix}
\end{align*}

La covarianza en este caso se debe resolver integrando la ecuación diferencial de Lyapunov donde $\Gamma_{X_a}(t)$ está en función del tiempo ($\dot{\Gamma}_{X_a} \neq 0$):

\begin{align*}
    \dot{\Gamma}_{X_{a}}(t) = A_a(t) \Gamma_{X_a}(t) + \Gamma_{X_a}(t) A_a(t)^T + Q_a
\end{align*}

\noindent donde $Q_a = B_a \left( S_0 \right) B_a^T$.\\

Como $\Gamma_{X_a}(t)$ varía en el tiempo, entonces $ \Gamma_Y(t)$ también varía en el tiempo.\\

\noindent \emph{Nota:} Ecuación diferencial de Lyapunov puede ser resuelta numéricamente utilizando métodos de integración temporal (por ejemplo, método de Euler).

